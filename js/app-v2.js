import { loadState, saveState, resetState, refreshDailyEnergy, getStoredUsername, setStoredUsername, clearStoredUsername } from './storage.js';
import { FlagGame } from './game.js';
import { loadProgress, recordAnswer, recordGame, saveProgress, accuracy } from './progression.js';
import { applyLanguage, getLanguage, t, setLanguage } from './i18n.js';
import { verifyPostingKey } from './steem-auth.js';
const $ = (id) => document.getElementById(id);
const GAME_STATE_PREFIX = 'steemflags.incompleteGame.v1_';
const gameStateKey = (username) => `${GAME_STATE_PREFIX}${encodeURIComponent(String(username).trim().toLowerCase())}`;
function loadIncompleteGame(username){if(!username)return null;try{return JSON.parse(localStorage.getItem(gameStateKey(username))||'null')}catch{return null}}
function saveIncompleteGame(username,game){if(username&&game.hasProgress())localStorage.setItem(gameStateKey(username),JSON.stringify(game.serialize()))}
function clearIncompleteGame(username){if(username)localStorage.removeItem(gameStateKey(username))}
async function loadComponent(){const r=await fetch(`./components/app-shell.html?v=20260817-20`,{cache:'no-store'});if(!r.ok)throw Error(`Unable to load component: ${r.status}`);return r.text()}
async function loadLeaderboard(){try{await import(`./leaderboard.js?v=${Date.now()}`)}catch(e){console.warn('Leaderboard failed',e)}}
async function fetchSteemBalance(account){const body=JSON.stringify({jsonrpc:'2.0',id:1,method:'condenser_api.get_accounts',params:[[account]]});for(const endpoint of ['https://api.steemit.com','https://api.steem.house']){try{const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body,cache:'no-store'});if(!r.ok)continue;const p=await r.json();const b=p?.result?.[0]?.balance;if(typeof b==='string')return b.replace(/\s*STEEM\s*$/i,'').trim()}catch(e){console.warn('STEEM balance endpoint failed',endpoint,e)}}return null}
async function refreshSteemBalance(account,element){if(!account||!element)return;const balance=await fetchSteemBalance(account);if(balance!==null)element.textContent=balance}
async function bootstrap(){try{$('app').innerHTML=await loadComponent();await loadLeaderboard();start()}catch(e){console.error(e)}}
function start(){let username=getStoredUsername(),state=loadState(username),progress=loadProgress(username);const loginView=$('loginView'),home=$('homeView'),lb=$('leaderboardSection'),gameView=$('gameView'),answers=$('answers'),feedback=$('feedback'),next=$('nextButton'),energy=$('energyValue'),sf=$('sfValue'),steem=$('steemValue'),assets=$('assetStats'),counter=$('questionCounter'),score=$('scoreLabel'),flag=$('flagImage'),newBtn=$('newGameButton'),resumeBtn=$('resumeGameButton'),summary=$('progressSummary'),menu=$('menu'),menuBtn=$('menuButton'),loginForm=$('loginForm'),usernameInput=$('usernameInput'),loginFeedback=$('loginFeedback'),loggedIn=$('loggedInUser'),logout=document.querySelector('[data-action="logout"]'),lang=$('languageSelect'),bar=$('quizProgressBar'),info=$('countryInfoLink');let key=$('postingKeyInput');if(!key){key=document.createElement('input');key.id='postingKeyInput';key.type='password';key.required=true;loginForm.insertBefore(key,$('loginButton'))}const game=new FlagGame(renderQuestion);let answered=false;
function hideInfo(){info.hidden=true;info.removeAttribute('href');info.setAttribute('aria-hidden','true');info.tabIndex=-1;info.style.setProperty('display','none','important')}
function basicUrl(name){const code=getLanguage()==='fa'?'fa':getLanguage()==='es'?'es':'en';return `https://${code}.wikipedia.org/wiki/${encodeURIComponent(String(name).trim().replace(/ /g,'_'))}`}
async function localizedUrl(name){const code=getLanguage()==='fa'?'fa':getLanguage()==='es'?'es':'en';if(code==='en')return basicUrl(name);try{const r=await fetch(`https://en.wikipedia.org/w/api.php?action=query&prop=langlinks&lllang=${code}&lllimit=1&format=json&origin=*&titles=${encodeURIComponent(String(name).trim().replace(/ /g,'_'))}`,{cache:'no-store'});const d=await r.json();const p=Object.values(d?.query?.pages||{});const title=p[0]?.langlinks?.[0]?.['*'];if(title)return `https://${code}.wikipedia.org/wiki/${encodeURIComponent(title.replace(/ /g,'_'))}`}catch{}return `https://${code}.wikipedia.org/w/index.php?search=${encodeURIComponent(name)}`}
function showInfo(name){info.textContent='ℹ️ More information about this country';info.href=basicUrl(name);info.hidden=false;info.setAttribute('aria-hidden','false');info.tabIndex=0;info.style.setProperty('display','block','important');localizedUrl(name).then(url=>{if(!info.hidden)info.href=url})}
function stats(){energy.textContent=state.energy;sf.textContent=state.sf;newBtn.disabled=!username||state.energy<=0;assets.hidden=!username;summary.hidden=!username;resumeBtn.hidden=!Boolean(username&&loadIncompleteGame(username));if(username)summary.textContent=`${t('gamesLabel')}: ${progress.games} · ${t('accuracyLabel')}: ${accuracy(progress)}%`;loggedIn.textContent=username?`${t('loggedInAs')} @${username}`:''}
function progressBar(n){const v=Math.max(0,Math.min(game.totalQuestions,Number(n)||0));bar.style.width=`${v/game.totalQuestions*100}%`;bar.setAttribute('aria-valuenow',v)}
function renderQuestion(q,points,n){answered=false;counter.textContent=`${t('question')} ${n} ${t('of')} ${game.totalQuestions}`;score.textContent=`${t('score')}: ${points}`;progressBar(n);flag.textContent=q.country[1];answers.replaceChildren();feedback.textContent='';feedback.className='feedback';hideInfo();next.hidden=true;q.options.forEach(([name])=>{const b=document.createElement('button');b.type='button';b.className='answer';b.textContent=name;b.onclick=()=>choose(b,name);answers.appendChild(b)})}
function restore(){if(!game.current)return;answered=game.answered;counter.textContent=`${t('question')} ${game.questionNumber} ${t('of')} ${game.totalQuestions}`;score.textContent=`${t('score')}: ${game.score}`;progressBar(game.questionNumber);flag.textContent=game.current.country[1];answers.replaceChildren();game.current.options.forEach(([name])=>{const b=document.createElement('button');b.type='button';b.className='answer';b.textContent=name;b.disabled=game.answered;if(game.answered&&name===game.current.country[0])b.classList.add('correct');b.onclick=()=>choose(b,name);answers.appendChild(b)});if(game.answered){feedback.textContent=`${t('score')}: ${game.score}`;showInfo(game.current.country[0]);next.hidden=false}else{hideInfo();next.hidden=true}}
function choose(button,name){if(answered||!username)return;const r=game.answer(name);if(!r)return;answered=true;progress=recordAnswer(progress,r.correct);if(r.correct){state.sf++;feedback.textContent=`${t('correct')} +1 SF`;feedback.className='feedback ok';button.classList.add('correct')}else{state.sf--;feedback.textContent=`${t('wrong')} ${r.answer}`;feedback.className='feedback bad';button.classList.add('wrong')}[...answers.children].forEach(b=>{b.disabled=true;if(b.textContent===r.answer)b.classList.add('correct')});saveState(state,username);saveProgress(progress,username);saveIncompleteGame(username,game);stats();showInfo(r.answer);next.hidden=false}
function showHome(){loginView.hidden=true;home.hidden=false;lb.hidden=false;gameView.hidden=true;hideInfo();stats();refreshSteemBalance(username,steem)}function showGame(){loginView.hidden=true;home.hidden=true;lb.hidden=true;gameView.hidden=false;hideInfo();refreshSteemBalance(username,steem)}
function newGame(){if(!username)return;state=refreshDailyEnergy(loadState(username),username);if(state.energy<=0){stats();return}clearIncompleteGame(username);state.energy--;saveState(state,username);game.reset();progress=recordGame(progress);saveProgress(progress,username);showGame();game.next();saveIncompleteGame(username,game);stats()}
function resume(){const s=loadIncompleteGame(username);if(!s||!game.restore(s)){clearIncompleteGame(username);stats();return}showGame();restore()}
loginForm.addEventListener('submit',async e=>{e.preventDefault();const u=usernameInput.value.trim().toLowerCase(),k=key.value.trim();if(!/^[a-z0-9.-]{3,32}$/.test(u)||!k){loginFeedback.textContent=t('invalidPostingKey');return}try{await verifyPostingKey(u,k);username=setStoredUsername(u);key.value='';state=refreshDailyEnergy(loadState(username),username);progress=loadProgress(username);showHome();logout.hidden=false;refreshSteemBalance(username,steem)}catch{loginFeedback.textContent=t('invalidPostingKey')}});
logout.addEventListener('click',()=>{clearStoredUsername();username=null;game.reset();showLoggedOut()});function showLoggedOut(){loginView.hidden=false;home.hidden=true;lb.hidden=true;gameView.hidden=true;logout.hidden=true;hideInfo();stats()}lang.addEventListener('change',()=>{setLanguage(lang.value);applyLanguage(document,getLanguage());if(!gameView.hidden&&game.current)restore()});newBtn.onclick=newGame;resumeBtn.onclick=resume;next.onclick=()=>{if(game.isComplete()){clearIncompleteGame(username);showHome();return}game.next();saveIncompleteGame(username,game)};menuBtn.onclick=()=>{menu.hidden=!menu.hidden};document.querySelectorAll('[data-action="home"]').forEach(b=>b.onclick=showHome);document.querySelectorAll('[data-action="reset"]').forEach(b=>b.onclick=()=>{clearIncompleteGame(username);state=refreshDailyEnergy(resetState(username),username);game.reset();showHome()});applyLanguage(document,getLanguage());if(username){state=refreshDailyEnergy(loadState(username),username);showHome();logout.hidden=false}else showLoggedOut();stats()}
bootstrap();